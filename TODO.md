# YO I'm the TO-DO list whatsup ??

## Somewhat by order of priority

- [ ] Render Engine & Physic Engine 
        wtf event is that ? link them to the Scene the same way as EventHandler maybe ? (not sure it's a good idea)

- [ ] Rethink the Rendering system
        i.e. where is window stored ? what actually calls the render methods ? (surely it should be the scene)

- [ ] Implement Entity's Depth
        i.e. an order for updating and rendering entities
